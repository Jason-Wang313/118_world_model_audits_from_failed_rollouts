# Submission Attack Log

## Attack 1: Strongest baseline selection

Mitigation: the strongest non-oracle baseline is selected after generation by hard-slice repair utility. It is `proposed_failed_rollout_audit_v4_1`.

## Attack 2: Success-only reporting

Mitigation: the paper reports success, repair utility, mechanism F1, invalid repair, repeat failure, damage, probe cost, calibration error, budget violation, paired-seed wins, ablations, stress sweeps, fixed-budget coverage/breach, and failure cases.

## Attack 3: Decorative components

Mitigation: removing failed traces, mechanism taxonomy, counterfactual replay, active probe value, repair memory, calibration gate, budget controller, freshness guard, or mechanism audit semantics reduces success or utility.

## Attack 4: Overclaiming ICLR readiness

Mitigation: all docs and the manuscript state `STRONG_REVISE`, not ICLR-main ready.

## Attack 5: Thin stress/failure audit

Mitigation: v5 includes `161,280` stress cells, `107,520` fixed-budget cells, and `24` failure cases.

## Attack 6: Citation quality

Mitigation: the manuscript uses real world-model, model-based RL, robot benchmark, conformal uncertainty, causal-confusion, and sim-to-real references with bright boxed clickable citation links.
